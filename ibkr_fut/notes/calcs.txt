notional exposure per contract (base currency) = capital required = multiplier * price * fx rate
tick value = multiplier * tick size
total return = spot return + dividends
excess return = total return - interest = spot return + dividends - interest
excess return = spot return + carry = spot return + dividends - interest
return (in price points), R^(points)_t = N_t-1 * (P_t - P_t-1)
cumulated return (in price points)_(t=0,T) = (P_t - P_0)
return (in currency of future), R^(instr)_t = R^(points)_t & multiplier
Return (base currency), R^(base)_t = R^(instr)_t * fx rate
R^(%)_t = 100 & R^(base)_t / capital_t-1
standard deviation of a series of returns = sqrt([1 / T](r_T - r^*)^2 + [1 / T](r_T-1 - r^*)^2 + [1/T](r_T-2 - r^*)^2 + ... [1  / T](r_1 - r^*)^2)
sharpe ratio = (mean return - risk-free rate) / standard deviation
SR of futures = mean excess return / standard deviation
lower percentile ratio = 1st percentile / 30th percentile
upper percentile ratio = 99th percentile / 70th percentile
relative lower fat tail ratio = lower percentile ratio / 4.43
relative upper fat tail ratio = upper percentile ratio / 4.43
current annualised standard deviation ($) = notional value * % standard deviation
SR = mean / standard deviation
mean = SR * standard deviation
annualised standard deviation = sigma
sigma(contract, base currency) = notional exposure(Base currency) * sigma_%
sigma(Position, Base currency) = sigma(Contract, Base currency) * N
sigma(target, Base currency) = Capital(Base currency) * target_risk
sigma(Target, Base currency) = sigma(Position, base currency)
N = Capital * target_risk / (Multiplier * price * fx rate * sigma_%)
Daily risk in price points sigma_p = (Price * sigma_%) / 16
sigma_p = Standard deviation(P_t - P_t-1, P_t-1 - P_t-2, ...)
N = (Capital * target_risk) / (Multiplier * FX * sigma_P * 16)
Contract leverage ratio = Notional exposure per contract / Capital
volatility ratio = target_risk / sigma_%
N = Volatility ratio / contract leverage ratio
Leverage ratio = total notional exposure / capital
Leverage ratio = volatility ratio
Maximum N = Capital / (Margin per contract / FX)
Maximum target_risk = (Multiplier * Price * sigma_%) / (Margin per contract)
Maximum target_risk = sigma_% * Maximum leverage ratio = sigma_% * (Maximum capital loss / worst return)
Capital = (N * Futures multiplier * price * FX * sigma_%) / target_risk
Minimum capital for 1 contract = (Multiplier * price * FX * sigma_%) / target_risk
Minimum capital for 4 contracts = (4 * Multiplier * Price * FX * sigma_%) / target_risk = 4 * notional exposure per contract / volatility ratio = (4 * Multiplier * Price_i,t * FX rate_i,t * sigma_%i,t) / (IDM * Weight_i,t * target_risk)
Half kelly = 0.5 * Expected SR
Trading decision: Calculate optimal position N each day, and round to the nearest whole contract
sigma(N)_t = sqrt([1 / N](r_T - r^*_t)^2 + [1 / N](r_T-1 - r^*_t)^2 + [1/N](r_T-2 - r^*_t)^2 + ... [1  / N](r_(t-N+1) - r^*_t)^2)

EWMA: r^*(lambda)_t = lambda(r_t) + lambda(1-lambda)(r_t-1) + lambda(1-lambda)^2(r_t-2 + ...)
exponentially weighted standard deviation: sigma_exp(lambda)_t = sqrt(lambda(r_t - r^*_t)^2 + lambda(1-lambda)(r_t-1 - r^*_t)^2 + lambda(1-lambda)^2(r_t-2 - r^*_t)^2 + ...)
for a given span in days N, calculate lambda as: lambda = 2 / (N + 1)
given some current level of sigma_t blended estimate = sigma_blend,t = 0.3(Ten year average of sigma_t) + 0.7(sigma_t)
SR = annual mean excess return / annualised standard deviation
Costs in SR terms = annual cost / annualised standard deviation = pre-cost SR - post-cost SR
Spread cost, price points = (Bid - Offer) / 2
Spread cost, currency = Multiplier * spread cost price points = tick value * (Spread in ticks) / 2
Total cost per trade, currency = spread cost currency + commission per contract
Total cost per trade, % = total cost per trade currency / (Price * Multiplier)
Risk adjusted holding cost = Risk adjusted cost per trade * rolls per year * 2
Risk adjusted transaction cost = risk adjusted cost per trade * turnover
Annual risk adjusted cost = risk adjusted holding cost + risk adjusted holding cost = transaction cost + holding cost = (Rolls per year * 2 + turnover) * Risk adjusted cost per trade
annual risk adjusted cost < 0.10
risk adjusted cost per trade < 0.01
Average daily volume in USD risk = FX rate * Average daily volume * sigma_% * price * multiplier
Optimal number of contracts for instrument i, given the proportion of capital(weight) allocated to it: N_i,t = Capital * Weight_i * target_risk / (Multiplier_i * Price_i,t * FX rate_i,t * sigma_%i,t)
Rough figure for IDM: target_risk / aggregate risk
N_i = Capital * IDM * Weight_i * target_risk / (Futures multiplier_i * Price_i * Fx rate_i * sigma_%,i)

Handcrafting:
    Divide the available instruments into asset classes. Allocated each asset class an equal fraction of the 100% in instrument weighting that is available
    Within each asset class, divide the available instruments into similar groups. Allocate each group within an asset class an equal fraction of the instruments weight share for that asset class.
    Within each group, allocate to each instrument within the group an equal fraction of the instrument weight share for that group
An approximation for IDM (preferably will calculate on our own): 1:1.00, 2:1.2, 3:1.48, 4:1.56, 5:1.7, 6:1.9, 7:2.10, 8 to 14:2.2, 15 to 24:2.30, 25 to 29:2.40, 30+:2.50
An algorithm for automatically selecting instruments:
    1. Decide on a set of possible instruments
    2. Choose the first instrument for the portfolio
    3. This forms the initial current portfolio. Measure the expected SR for this portfolio, using the instrument cost
    4. Iterate over all instruments not in the current portfolio, considering each in turn as a potential new instrument:
        i. Construct a trial portfolio consisting of the current portfolio and the potential new instrument
        ii. allocate instrument weights for the trial portfolio. calculate an IDM.
        iii. check the minimum capital requirement for all instruments. if not met, skip this instrument.
        iv. measure the expected SR for this trial portfolio using the instrument weights, correlation matrix, and instrument cost
    5. Choose the instrument which produces the highest trial portfolio SR from step 4. Replace the current portfolio with the trial portfolio. The new current portfolio will consist of the old current portfolio plus this new instrument.
    If the expected SR for the new current portfolio is 10% lower than the highest current portfolio SR seen so far, stop. Otherwise, continue again from step 2
Choosing the first instrument: We need to make some assumptions about what roughly the final portfolio will look like. For example, we might expect that we'll end up with about ten instruments. With ten instruments equally weighted, we would have an instrument weight of 10%. It isn't critical to get these initial guesses exactly right, but they are necessary to ensure we don't select a first isntrument with a very high minimum capital.
Assume that all instruments have the same pre-cost SR so that their post-cost SR depends only on costs
If each instrument sub-system has the same notional pre-cost annual SR*, and if the estimated turnover for each instrument is equal to T, then the net SR will be = pre-cost SR - costs: turnover multiplied by the risk adjsuted cost for the instrument, c_i
Instrument annual SR_i = SR^* - (T * c_i)
Instrument annual mean_i = target_risk * [SR^* - T * c_i]
Instrument annual mean in portfolio_i = weight_i * IDM * target_risk * [SR^* - (T * c_i)]
Portfolio mean = Sum_i(Weight_i * IDM * target_risk * [SR^* - (T * c_i)])
Portfolio sigma = IDM * target_risk * sqrt(w SUM w')
Portfolio SR = Sum_i=0...N(Weight_i * [SR^* - (T * c_i)]) / sqrt(w SUM w')

Formula for regressing the monthly aggregate returns of backtested portfolio (y) on the benchmark buy and hold portfolio with variable risk position sizing (x) using the standard linear regression formula y_t = a + Bx_t + epsilon_t

EWMAC: EWMA (N = 64, lambda = 0.031)_t = 0.031p_t + 0.031(1-0.031)p_t-1 + 0.031(1-0.031)^2p_t-2 + ...
EWMA (N = 256, lambda = 0.0078)_t = 0.0078p_t + 0.0078(1-0.0078)p_t-1 + 0.0078(1-0.0078)^2p_t-2 + ...
EWMAC(64, 256) = EWMA(N=64)_t - EWMA(N=256)_t
Historical trading cost = Current cost * (Historical sigma_p / current sigma_p)

N_i,t = (Sign(trend)_t * Capital * IDM * Weight_i * target_risk) / (Multiplier_i * Price_i,t * FX_i,t * sigma_%,i,t)
Raw forecast = (Fast EWMA - Slow EWMA) / sigma_p
Scaled forecast = Raw forecast * 10 / (Average absolute value of raw forecast)
Forecast scalar = 10 / (Average absolute value of raw forecast)
Scaled forecast = Raw forecast * forecast scalar
For the EWMAC(16,64) trading rule, carver estimated the forecast scalar to be approximately 1.8. To avoid in sample cheating, we must use a purely backward looking rolling estimate of the forecast scalar when backtesting. These scalars are relatively stable over time so there is no harm in using the fixed values he provides for each strategy
For this strategy: Scaled forecast = [(EWMA(16) - EWMA(64)) / sigma_p] * 1.9
Using scaled forecasting for optimal position sizing: N_i = Scaled forecast_i,t * Capital * IDM * Weight_i * target_risk / (10 * Multiplier_i * Price_i,t * Fx_i,t * sigma_%i,t)
Maximum forecast value of 20: Capped forecast_i,t = Max(Min(Scaled forecast_i,t, +20), -20)
N_i,t = Capped forecast_i,t * Capital * IDM * Weight_i * target_risk / (10 * Multiplier_i * Price_i,t * FX_i,t * sigma_%i,t)

EWMAC(16, 64): EWMA(N=16, lambda=0.118)_t = 0.118p_t + 0.118(1-0.118)p_t-1 + 0.118(1-0.118)^2p_t-2 + ...
EWMA(N=64, lambda = 0.031)_t = 0.031p_t + 0.031(1-0.031)p_t-1 + 0.031(1-0.031)^2p_t-2 + ...
Estimate for forecast scalar here is 4.1 for EWMAC(16,64)
Buffer B is set as a fraction, F, of the average long position (i.e. the position we'd take for benchmark strategy): B_i,t = F * Capital * IDM * Weight_i * target_risk / (Multiplier_i * Price_i,t * FX_i,t * sigma_%i,t)
Theoretically the correct value of F depends on several factors, including the speed and profitability of the relevant trading rule, and the costs we have to pay, but it's most convenient to set it at a fairly conservative value of 0.10
Lower buffer, B^L_i,t = round(N_i,t - B_i,t)
Upper buffer, B^U_i,t = round(N_i,t + B_i,t)
Given a current position in round contracts of C: 
    B^U_i,t <= C_i,t <+ B^L_i,t: No trading required
    C_i,t < B^L_i,t: Buy (B^L_i,t - C_i,t) contracts
    C_i,t > B^U_i,t: Sell (C_i,t - B^U_i,t) contracts

EWMAC strategy: Trade a portfolio of one or more instruments, each with positions scaled for a variable risk estimate. Calculate a number of forecasts for different speeds of trend filter. Place a position based on the combined forecast.
Speeds:
    EWMAC(64,256)
    EWMAC(32,128)
    EWMAC(16,64)
    EWMAC(8,32)
    EWMAC(4,16)
    EWMAC(2,8)

Forecast scalars for different speeds of trend filter (estimates):
    EWMAC2 : 12.1
    EWMAC4 : 8.53
    EWMAC8 : 5.95
    EWMAC16 : 4.10
    EWMAC32 : 2.79
    EWMAC64 : 1.91

Combining different forecasts: Take a weighted average of capped forecasts from different trading rules. The weights used when calculating this average are forecast weights. Just like instrument weights, the forecast weights will sum to 1 and none will be negative.
For each trend filter j we are trading, we calculate a capped forecast at time t using the equations below on a given instrument i that we are trading:
raw forecast_i,j,t = (Fast EWMA_i,j,t - Slow EWMA_i,j,t) / sigma_p,i,t
We then apply a filter specific forecast scalar (which is the same as all instruments), and a cap of 20 on the absolute value of the scaled forecast:
Scaled forecast_i,j,t = Raw forecast_i,j,t * Forecast scalar_j
Capped forecast, f_i,j,t = Max(Min(Scaled forecast_i,j,t, +20), -20)
We now take the weighted average of these capped forecasts, using forecast weights w_i,j summing to 1 that are specific to a given instrument:
raw combined forecast_i,t = w_i,1 * f_i,1,t + w_i,2 * f_i,2,t + w_i,3 * f_i,3,t + ...

Potential factors determining instrument weights outlined in benchmark strategy:
    the expected pre-cost SR: better performing instruments should get a higher weight (in theory, but carver found no significant differences between instrument sub-strategy SR).
    the costs of trading a given instrument: cheaper instruments will get a higher weight, since their post-cost SR will be higher (in theory, but once we've excluded expensive instruments costs had almost no effect on weights)
    the correlation between instruments: we want a higher weight to instruments that are more diversifying

These factors are the same for forecast weights:
    the expected pre-cost SR: better performing trading rules should get a higher weight. Potentially, certain rules will perform differently for different instruments
    the costs of trading a given filter: cheaper and slower trading rules will get a higher weight, since their post-cost SR will be higher. These costs will be different for each instrument.
    The correlation between forecasts: we want a higher weight to rules that are more diversifying. Correlations for trading rule varations should be similar, regardless of the instrument that they are measured on.

Method for allocating weights:
Forecast weights should be the same across instruments, except where trading costs dictate otherwise.
Forecast weights should be lower on rules that trade faster where instruments have a higher cost per trade.
We should avoid over fitting forecast weights: placing too much weight on a single trading rule variation.
Forecast weights should ideally be higher on more diversifying rule variations: the slowest and the fastest

Firstly, on an instrument by instrument basis, we remove trading rules that are too expensive. Secondly, we allocate forecast weights using a top down methodology (handcrafting). For this strategy, that implies that all remaining trend filters get the same forecast weight.

Removing expensive trading rules:
We do not want to trade any instrument which uses up more than a third of our expected pre-cost return in costs.
Breaking down annual risk adjusted costs = transaction cost + holding costs:
risk adjusted transaction costs = risk adjusted cost per trade * annual turnover
Risk adjusted holding costs = risk adjusted cost per trade * rolls per year * 2
A limit of 0.10 SR units is appropriate for instruments, but for trading rules we can have a higher maximum for a couple of reasons. Firstly, if our most expensive trading rule incurs exactly 0.10 SR units in costs for a given instrument, then on average we'll have a combined forecast which always comes in well under 0.10 SR units. Secondly, the effect of buffering our positions will slow down our trading and allow us to trade a set of forecasts that is a little quicker. Because of these two effects, we use a higher limit of 0.15 SR units for selecting trading rules.
LIMIT OF 0.15 SR units for selecting trading rules.

For a given instrument if we set a maximum of 0.15 SR units in costs, then we can derive the maximum allowable turnover for a given trading rule variation:
Annual risk adjusted costs < 0.15
(Cost per trade * Turnover) + (Cost per trade * rolls per year * 2) < 0.15
Turnover < [0.15 - (Cost per trade * rolls per year * 2)] / cost per trade

Top down method for choosing forecase weights:
    Split the list of trading rules by style. Allocate between styles according to preferences
    Within a given style, allocate equally across trading rules
    Within a given trading rule, allocate equally across variations of each given trading rule

Trading strategies are either divergent or convergent. Divergent strategies make money when markets diverge away from equilibrium: trend following is a classic. Convergent strategies make money when markets move towards their equilibrium.

Applying top down method to ewmac strategy:
    Allocate 100% of our forecast to weights of our one style (divergent)
    We only have one trading rule: trend following with EWMAC. Allocate 100% of our forecast weights to that rule.
    We have between one and six variations of our trading rule, depending on the instrument we're trading. Equally divide 100% of our forecast weight between these rules.

As an example, suppose that for a given intsrument we drop the fastest variation of the EWMAC rule: 2,8. We then allocate a 20% forecast weight in each of the five slowest variations.

The below table shows the appropriate FDM and forecast weights for a given set of trend filters. We should select the appropriate row for a given instrument, given the set of trading rule variations that we have identified as cheap enough to trade.
                                Forecast weight for each rule variation        FDM
EWMAC2, 4, 8, 16, 32, and 64                    0.167                          1.26
EWMAC4, 8, 16, 32, and 64                       0.2                            1.19
EWMAC8, 16, 32, and 64                          0.25                           1.13
EWMAC16, 32, and 64                             0.333                          1.08
EWMAC32 and 64                                  0.50                           1.03
EWMAC64                                         1.0                            1.0

With an FDM applied, it's now possible for the combined forecast to exceed an absolute value of 20. I'm not comfortable with that due to the likely risk, so I want to also apply a cap if the combined forecast were less than -20 or greater than +20

To recap:
We calculate a weighted average of capped forecasts for various trading rule variations, using forecast weights w summing to 1 that are specific to a given instrument, and which depend on the trading costs and turnover:
Raw combined forecast_i,t = W_i,1 * F_i,1,t + W_i,2 * F_i,2,t + W_i,3 * F_i,3,t + ...

To account for diversification we now apply a forecast diversification multiplier for the given instrument FDM_i:
Scaled combined forecast_i,t = Raw combined forecast_i,t * FDM_j

As we did for individual filters, to avoid a situation when we have too much risk in a given instrument, we cap the combined forecast:
Capped combined forecast_i,t = Max(Min(Scaled combined forecast_i,t, +20), -20)

Finally, we can plug the capped combined forecast into the now familiar position sizing equation, after which we would apply buffers and decide whether any trading was required:
N_i,t = Capped combined forecast_i,t * Capital * IDM * Weight_i * target_risk / (10 * Multiplier_i * Price_i,t * FX_i,t * sigma_%i,t)


Dynamic optimisation (for when you can't trade the jumbo portfolio):
Every day we will calculate the best possible set of positions that can be held, given that positions in futures markets can only be integers. The best set of positions are those that most closely match the optimal, unrounded portfolio of positions.
We need to introduce some formal notation. Suppose that we have a set of optimal unrounded positions in each instrument, where N_i is the unrounded number of contracts of instrument i that we would ideally hold. These will have been calculated using the trading capital that we have available, using combined forecasts, instrument weights, and IDM. Importantly, these positions will be before buffering has been applied, since buffering requires knowledge of the positions we currently hold.
It turns out to be more natural to perform this optimisation using portfolio weights, where a portfolio weight is equal to the notional value of a position as a proportion of our trading capital. 
We define a portfolio weight for a given instrument as:
optimal unrounded portfolio weight, w_i = N_i * Weight per contract
Weight per contract_i = notional exposure per contract_i / capital
notional exposure per contract_i = multiplier_i * price_i * fx rate_i
Our objective is to find the best set of weights, w^*_i, for which we minimise the standard deviation of the tracking error portfolio.
This standard deviation is calculated using the two sets of weights and the covariance of asset percentage returns. Furthermore, the covariance is calculated from the correlation and annualised standard deviation of percentage returns for the underlying instruments, not the trading sub-strategies

Tracking error weight e_i = w^*_i - w_i
Vector of tracking errors e = [e_0, e_1, e_2, ...]
covariance of asset percentage returns = SUM = sigma^T.p.sigma
standard deviation of tracking error portfolio = sqrt(e^T.SUM.e)
Note that T is the transposition operator, and the dots (.) in the final formula are matrix dot multiplication
Because we can only take integer positions, our w^* must be in whole units of weight per contract. So, for example, if the weight per contract in any given instrument is 0.32, then we can only have a w^* which is one of the following values: ..., -0.96, -0.64, -0.32, 0, +0.32, +0.64, +0.96, ... and so on.
The correlation matrix p and the annualised standard deviation sigma are both calculated from the percentage returns of all the instruments we are trading. Recall that we calculate daily percentage returns in the following way: given a back-adjusted price, p, and the price of the traded futures contract on the appropriate day, F:
Percentage return, r_%t = (p_t - p_t-1) / F_t-1
To find the standard deviation, we use the standard method introduced before: a EWMA with a 32-day span. For correlation, a longer look back is appropriate: we use the last six months of weekly returns.

We will use the greedy algorithm. To use this method to solve the optimisation, we can use the following approach:
    1. come up with a set of starting weights. This is equal to a zero weight in all instruments. These starting weights become the current best solution.
    2. Find the proposed set of weights that is the best we can do, starting from the current best solution.
    3. We set the proposed solution to be initially equal to the current best solution.
    4. Loop over all the instruments which we can have weights for. For each instrument calculate an incremented solution:
        i. If the optimal unrounded weight for that instrument is positive, then add the weight per contract to the current best solution weight for this instrument. This is equivalent to buying an additional contract for that instrument.
        ii. If the optimal weight for that instrument is negative, then subtract the weight per contract from the current best solution weight for this instrument. This is equivalent to selling an additional contract for that instrument.
        iii. Evaluate the tracking error for the incremented solution. If it is better than the proposed solution, then set the proposed solution to be equal to the incremented solution.
    5. After completing the instrument loop you will have the best proposed solution. This will be the best solution we can have that involves adding or subtracting one contract for a single instrument.
    6. If the best proposed solution has a lower tracking error than the current best solution, then it becomes the new current best solution. Repeat from step 4.
    7. If the best proposed solution is the same as the current best solution, then the current best solution is considered optimal. Stop.

Notice that in step 4, we don't increment the proposed solution; instead we are always starting from the baseline of the current best solution.
The final job is to convert the optimised weights back into positions. These positions will always be integers, so no rounding is required.
Optimised position N^*_i = Optimised portfolio weight w^*_i / weight per contract_i

Carver has run this optimisation with countless examples and the results are fascinating and intuitive. For example, if an instrument has a large minimum capital and we can't take a position in it, it will take positions in instruments with smaller weights per contract that are correlated and have the same sign of position on (long or short).
It also has a number of advantages over more complex optimisation methodologies. Firstly, it is very robust and unlikely to generate sparse portfolios consisting of positions in just one or two instruments. Similarly, it won't take potentially dangerous hedged positions since the optimised position will always be in the same direction as the forecast.
The main disadvantage is that it may miss a slightly better set of optimal weights that will give a small improvement on the tracking error. But this is a small price to pay, since these optimal weights are often less robust.

Dealing with costs: a cost penalty
There is one important difference between dynamic and static optimisation. The static optimisation assumes that all instruments will have the same pre-cost SR. This makes sense, since there was insufficient evidence that any one instrument will perform better than another in a given strategy, over a sufficiently long backtest. In contrast the dynamic optimisation is using underlying positions that have been scaled for forecast strength.
Since we assume that forecast strength is a good predictor of risk adjusted returns in the near future, this means the dynamic optimisation is effectively incorporating different estimates for SRs for each instrument into its calculations.
The dynamic optimisation has no concept of costs. Fortunately, it is possible to modify the tracking error equations to include costs:
Optimal unrounded portfolio weight w_i = N_i * Weight per contract_i
Previous position as number of contracts held = P_i
Optimised portfolio weight w^*_i = N^*_i * Weight per contract_i
Previous (current) weight w^P_i = P_i * Weight per contract_i
Cost in weight terms w^c_i = (C_i / Capital) / Weight per contract_i
Tracking error weight e_i = w^*_i - w_i
Vector of tracking errors e = [e_0, e_1, e_2, ...]
Trade as a weight Delta_i = abs(w^P_i - w^*_i)
Cost of all trades Delta = sum(Delta_0 * w^c_0 + Delta_1 * w^c_1 + Delta_2 * w^c_2 + ...)
Covariance of asset percentage returns = Sum
Tracking error, standard deviation and cost penalty = sqrt((e^T.Sum.e) + 50Delta)

New terms introduced in these equations:
currently held, or previous, number of contracts P_i in each instrument i
The position weight implied by those previous contracts, w^P_i
The estimated cost of trading a single contract of instrument i, C_i, in the same currency as our capital
That cost translated into the cost of trading a given weight, as a proportion of capital w^c_i
Trades in weight terms, Delta_i
The cost of all trading in weight terms, Delta (greek symbol)
The total cost is multiplied by 50 and then added to the tracking error. Since we are trying to minimise the tracking error, this means we will seek out optimised sets of weights which have a lower cost of trading.

How do we calculate the cash cost of trading a single contract, C_i? The same way as foundational equations:
Spread cost, price points = (Bid-Offer) / 2
Spread cost, currency = Multiplier * Spread cost price points
Total cost per trade, currency = Spread cost currency + commission per contract

For backtesting, we will need to adjust the historical costs in currency terms, using the method described earlier.

Dealing with costs: buffering
Using a cost penalty reduces the turnover of the dynamic optimisation by some margin, but it is still higher than you would expect. The main reason for this is that the optimisation only ever puts on a position in the same direction as the underlying forecast, long or short. As a result, whenever a forecast changes sign from long to short, we will always close our entire position in that instrument (and possibly open a new position in the opposite direction).
This behavior is unaffected by the imposition of cost penalty, and results in a lower bound below which turnover can't be reduced. It turns out this lower bound is actually quite high. Increasing the cost penalty further to try to reduce turnover only results in trading being limited to the very cheapest instruments, and heavily penalises after cost performance.
However, there is a solution at hand. Remember that we inadvertently dropped another element of our original trading system which helps to reduce costs: buffering. Back in earlier strategies, we set the trade buffer at 10% of the average position that we expect to hold. We then constructed a buffer zone around the optimal position that we wanted to hold. If a current position were outside this zone then we would trade to the edge of the buffer. But if the position was inside the buffer zone then no trading was necessary.
It didn't make sense to use buffered positions as an input into the dynamic optimisation, so the unrounded positions we are trying to match are calculated before a buffer is applied. Unfortunately, it isn't practical to do this type of buffering on individual positions in the dynamic optimisation.
But we can implement buffering on the entire portfolio by using the general idea of buffering, but in tracking error units rather than position units.
To achieve this, we first set a tracking error buffer in standard deviation terms, B_sigma. The tracking error is always positive, because it is the sum of two positive figures: an annualised standard deviation, and annualised costs. So our modified buffering methodology will look like this:
    - Calculate the optimised portfolio of unrounded positions using the dynamic optimisation method.
    - check the tracking error of the optimised portfolio versus the positions we already hold.
    - if this tracking error is smaller than the buffer, then no trading is required.
    - if this tracking error is larger than the buffer, then trade until tracking error is equal to the buffer.

The analogous equivalent to the average position, used in the original buffer, is our average risk target. This is measured as an annualised standard deviation of returns. The 10% symmetric buffer we used for position buffering is equivalent to a 5% asymmetric buffer for tracking error. This gives us the following calculation for an equivalent tracking error buffer:
B_sigma = 0.05 * target_risk
When using a 20% risk target, the optimal buffer B_sigma is 1%

Next, we calculate the tracking error of our current portfolio against the dynamically optimised portfolio. Note: this is not the same as the tracking error of the optimised portfolio against the original optimal unrounded positions. We are now trying to see if the current portfolio is sufficiently similar to the optimsed portfolio that no trading is necessary.
This tracking error consists purely of the standard deviation and doesn't include the cost penalty, since we've already used costs in determining what the optimised portfolio will be:
Optimised portfolio weight w^_i = N^*_i * Weight per contract_i
Previous weight w^P_i = P_i * Weight per contract_i
Tracking error current weight e^p_i = w^*_i - w^p_i
Vector of tracking errors e_p = [e^p_0,e^p_1,e^p_2, ...]
Tracking error, T = sqrt((e_p)^T.SUM.e_p)

If we were to trade all the way to the new optimsed weights, we would get a tracking error of zero. But as we did earlier with position buffering, we actually want to trade to the edge of the buffer, i.e. trade in such a way that our tracking error is equal to the size of the buffer, 1%.
To achieve this we calculate an adjustment factor, which is the distance we need to travel from the current portfolio such that the tracking error is equal to the buffer. If the tracking error of the current versus optimised portfolio is T, then:
Adjustment factor, a = max([T-B_sigma] / T, 0)
In an example considering T = 2.35%, we have: Adjustment factor, a = max([2.35% - 1%] / 2.35%, 0) = 0.57
We then need to multply the trades that would be required to trade all the way to the optimsed portfolio by a: Required trade_i = round(a * [N^*_i - P_i])

EXAMPLE WITH EXAMPLE VALUES:
For example, to achieve the optimised portfolio weight for S&P 500 (0.08) without considering buffering, we would need to buy 0.08 - 0 = +0.08 weight units of S&P500. This corresponds to two contracts, since the weight per contract is 0.04. If we multiply the required trade of +0.08 by 0.57, then we get a trade of +0.0456. Similarly, the required trade in the US 10-year is reduced from +0.24 to +0.138. Since the required trade in US 5-year is zero, this is unchanged.
Next, we convert the required trades into contract units. For S&P500, +0.0456 / 0.04 = +1.14 and for US 10-year bonds +0.138 / 0.24 = +0.57. Finally, these required trades are rounded to whole contracts to produce the trades we will actually do. We buy one S&P 500 contract, and a single US 10-year bond future, ending up with weights of [0, 0.24, 0.04] instead of the optimised weights of [0, 0.24, 0.08]
Notice that the required trade is virtually identical to what we would do without buffering, except we buy one less S&P 500 future. 

Other benefits from dynamic optimisation:
We could include additional futures in the set of instruments which we generate potential optimal positions for, but then not actually allow yourself to hold optimised positions in those instruments.
These could be instruments which are too costly to trade, have insufficent liquidity, or for which there are trading restrictions. In carver's portfolio, he generates positions for approximately 150 futures, but only allows himself to trade around 100.
The advantage of this is that we will effectively get a better forecast by combining information from more instruments. For example, we know that most trends happen across an asset class. It might be that there is a decent trend in a particular group of assets, but there is a much stronger trend in instruments which we can't trade. By using dynamic optimisation we can transfer risk to correlated markets which we can trade, even if they don't have strong forecasts in their own right.
We can also set position limits in the dynamic optimisation. For example, we might not want to hold more than 10 VIX contracts.
There might be a situation where we are temporarily unable to trade a given instrument, for example because it has idiosyncratic market holiday, but where you wanted to reduce risk and cut an existing long position. Again, this can be dealt with by setting a minimum and maximum position limit for a given instrument, equal to its current position.
The optimisation will do a proxy trade in instruments whose markets are currently open, either selling a positively correlated instrument or buying a negatively correlated instrument if one is available, and has the appropriate sign on its forecast.
Another advantage comes when we come to roll our futures contracts. In the traditional strategy, we assume that we want to hold the same positions after the roll. But in a dynamic strategy, it might sometimes make sense to force an instrument that is about to roll to close. Then the optimisation will subsequently decide if opening the same position again in that instrument makes sense, or if it would be optimal to trade something else. This is particularly useful if we find a given instrument is very expensive to roll.